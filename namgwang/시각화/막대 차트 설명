<막대 차트 설명>

[d3 기본 문법]
d3.select - 특정 태그 하나를 선택
d3.selectAll - 특정 태그 전체를 선택
selection.attr - 선택 태그의 속성값 지정
selection.data - 차트에 사용할 데이터 가져옴
selection.enter - 데이터 개수만큼 태그가 부족할 시 추가
selection.append - 새로운 태그 추가.
.attr는 append한 태그에 해당 어트리뷰트를 추가하는 것.

[설명]
chart 함수로 차트를 그리는데.

svg는 위에 svg로 선언해둔 부분을 쓴다.
첫번째 g 태그를 만들어서 transform의 translate를 x축으로 margin.left 만큼 옮겨주고
rect를 만들기 위해서 selectAll을 통해 특정 태그 전체를 선택한다.
(존재하지 않는 태그를 선택하는 거에요. 원래 이렇게 함)
그런 다음에 사용을 하려는 데이터를 data 에다 넣어서 가져온다.
join을 통해 rect를 실제 사용하는 듯...
fill로 원하는 색을 지정해주고
x 위치, y 위치를 잡아주는데
좌표가 왼쪽 위가 (0, 0) 이고 오른쪽 아래가 (n, n)이다.
y 값을 저렇게 bottom - data[i].value로 하는 이유는
사각형 맨 위를 그리기 시작할 위치를 찾는 거라고 생각하면 되고
(그래서 높이가 낮은 사각형일수록 y 값이 크다. 위 공백이 큼.
100에서 빼줬는데 이것도 뭐.. 어디에 위치할거냐에 따라서
다른 값으로 바꿔주면 되는거겠다.)
height는 실제 사각형의 높이
width는 사각형의 너비가 된다.

두 번째 g는 사각형 위에 적히는 값을 의미한다.
selectAll text해서 텍스트 전체를 선택하고
마찬가지로 데이터를 가져온다.
또 join하고
x, y 값을 설정해준다. 아까와 동일함.
dy는 굳이 안해도 되기는 함. 그냥 위치 잡아주는 역할인데 중요하진 않다.
그리고 text로 각 값을 적어주면 된다.

세 번째 g는 x축이고
네 번째 g는 y축이다.
call은 뭔지 모르겠음 ^0^

그 아래는 x축이랑 y축 만들 때 필요한 값들인 것 같은데..
저런 것들이 필요하구나 하고 알고 쓰면 될 듯.
x축의 range 값은 0에서 610으로 잡아서 610 길이의 축이 그려지는 것.
y축 같은 경우는 100에서 20으로 잡았는데 위치 20부터 100까지 축을 그리는 거인 듯 하다.
20이라는 거는 지금 80이 제일 최대 값이기 때문에 맞게 그린다고 잡은 거다. 
음.. 조절하면 됨.

실제 축을 그리는 거는 xAxis랑 yAxis 인 듯.
위치 잡아주고.. 등등 뭐가 많은데
거의 복붙한 거기 때문에 자세한 내용은 모르겠지만 저런 게 필요한 듯.
yAxis 쪽에 많은 내용을 주석처리 했는데
있으나 없으나 영향이 없어서 없애도 될 것 같아 없앤 거다.
실제 가져온 데서는 저것들이 다 있었다.
